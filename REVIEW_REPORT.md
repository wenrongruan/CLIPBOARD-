# SharedClipboard 项目审查报告

日期：2026-07-05

本报告旨在对 SharedClipboard 项目进行全面审查，重点评估客户端架构、云端同步、数据传输安全性及稳定性，并检查代码与核心产品策略文档的一致性。

## 1. 架构与主执行路径评估

### 1.1 AppContext 与延迟加载
项目采用 `AppContext` (Service Registry) 模式装配所有核心服务，且通过多阶段初始化和 `QTimer.singleShot`（在 `main.py` 中）延迟了非核心路径（如云同步、文件同步、插件加载）的启动。这种设计有效保证了“首屏闪电启动”的 `本地优先` 策略，避免了无网络环境或插件解析阻塞主线程。

### 1.2 Qt 事件循环与插件执行
主界面通过 `MainWindow` 串联事件，而耗时的插件操作交由 `PluginWorker` (`QThread`) 异步执行。超时机制与 Cancellation 支持较为完备，未发现明显的死锁风险。这种分离保证了 GUI 层响应迅速。

## 2. 云同步与数据传输评估

### 2.1 同步队列机制
`CloudSyncService` 使用了基于 `deque` 的 `_pending_upload_queue` 维护离线写入，队列满时会主动丢弃最旧数据并抛出警告 (`queue_overflow`)。这种设计兼顾了内存安全和极端掉线场景下的防御，但可能会导致用户在长时间离线恢复后出现数据缝隙（丢失被挤出的数据）。建议在抛出 `queue_overflow` 时，不仅发信号，还要尝试在本地库标记这批数据的上传状态为失败或需要全量对比。

### 2.2 Token 刷新并发安全
`HttpClient.refresh_token` 中正确使用了 `threading.Lock` 实现了“双重检查锁”机制，这对于一个在背景进行高并发 `pull/push` 工作的服务至关重要。这有效避免了多个工作线程因 401 触发并发换 token 导致服务端 `refresh_token` 过期/冲突，从而保障了稳定的登录态。

### 2.3 异常状态管理与同步节流
`CloudSyncService` 在处理 401（认证失败）或 403（空间越权）时设计了良好的节流退避机制。然而游标更新（cursor）既有 `settings.json` 落盘兜底，又新增了 `app_meta` 表处理，其后向兼容代码略显冗杂。文件同步中的 `upload_finished` 等信号在成功失败状态上转发生效，能正确通知 UI。

## 3. 数据传输的边界与安全性
1. **Presigned URL 校验**：对于云端下发的文件和图片下载 URL，在 `_validate_storage_url` 进行了严格的白名单域名过滤，杜绝了中间人替换 URL 请求恶意内网或跨域端点的风险。
2. **大文件防爆内存**：明确规定了 `FILE_SIZE_HARD_LIMIT` 和分片机制 `multipart`，并且空字段（如 empty tags）在上传时剔除，大大优化了 payload 大小。
3. 遵循本地优先的策略，无论云端是否返回 500 甚至超时，均不会阻塞用户获取本机剪贴板历史。

## 4. 文档一致性与清理建议

经过比对代码仓库实际现状，部分文档存在与最新逻辑、版本不一致的情况。

- **`PRODUCT_STRATEGY.md` 与 `README.md`** 中存在双重序号。代码层面文档描述的团队（Team）机制已经实装在 `CloudSyncService` 中，订阅级别（Super/Ultimate）编辑 Team Space 逻辑代码吻合，这些策略保持一致。
- 自动化测试用例目前共计 **304 个**（分布在 32 个测试文件中），建议在 `README.md` 中同步更新测试数量描述以保持一致。
- ~~部分废弃规划文档（`docs/superpowers` 中的草案）建议存档或删除。~~ ✅ 已清理完成。

## 5. 建议与总结

- **优势**：本地与云端架构解耦得非常好，容灾机制强，代码健壮性较高。
- **隐患**：本地上传队列（`_pending_upload_queue`）满时的静默丢弃虽然保护了内存，但可能破坏数据强一致性，建议考虑在本地数据库中增加 `sync_status` 字段记录待同步状态。
- **清理动作**：将修改 `README.md` 等处不一致的测试条目数量描述。

结论：此项目在“云同步”与“本地优先”的主线交接点上表现出色，适合继续稳定迭代。
