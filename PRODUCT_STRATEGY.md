# SharedClipboard 产品策略

本文档只描述基于当前代码的产品方向，不再包含外部市场估算或未经验证的竞品结论。

## 1. 当前产品定位

主叙事：**复制过的东西，再也不用找第二遍。**
副叙事：**本地优先，可选同步；安静常驻，需要时出现。**

SharedClipboard 是一个本地优先的跨平台剪贴板历史管理器，主路径是：复制 → 自动记录 → 热键唤出 → 搜索/点击 → 复制回去。围绕这条主路径，产品的设计目标是：

- 让首次启动到第一次找回历史的体验在 60 秒内完成。
- 默认安静、低打扰，不在主路径里持续推送升级或登录。
- 团队、分享、文件同步、插件商店等扩展能力分层暴露，不抢主路径。

## 2. 当前已经落地的能力

从代码看，当前产品已经具备这些能力：
- 剪贴板历史、搜索、收藏、分页和置顶。
- SQLite 本地库，支持 WAL 和 `busy_timeout`。
- MySQL 后端与云端同步入口。
- 设置页中的数据库、过滤、语言、热键和插件管理。
- 插件系统，包括加载、配置、启用/禁用、卸载、商店安装和执行反馈。
- 内置插件 `smart_text` 与 `ai_image_gen`。

## 3. 核心用户假设

这里的用户画像是产品方向上的内部假设，不是市场统计结论：
- 开发者：需要处理文本、代码片段和大量复制粘贴。
- 办公用户：需要快速回溯历史剪贴板、收藏重要内容。
- 设计或内容创作者：需要在文本和图片之间切换。
- 小团队用户：需要本地优先或自建同步方案。

## 4. 差异化方向

代码已经体现出的差异化方向有三条：

### 4.1 本地优先

应用默认能独立运行，数据先落本地库，再根据配置选择 MySQL 或云端同步。这样可以降低启动门槛，也方便个人用户直接使用。

### 4.2 可扩展

插件系统已经不是概念设计，而是实际实现。后续新增能力应优先考虑：
- 文本处理
- 图像处理
- AI 辅助
- 企业定制扩展

### 4.3 可控同步

同步能力已经拆成三个层次：
- `local`
- `mysql`
- `cloud`

这意味着产品策略上可以继续强调“可选同步”和“用户控制”，而不是把云端作为唯一入口。

## 5. 近期优先级

P0：收紧主路径、降低首屏认知负担。

1. 首次启动 3 步引导：复制 → 热键唤出 → 找回。
2. 主界面默认聚焦剪贴板列表与搜索；团队/文件/插件入口退到二级。
3. 右键菜单分组：复制/收藏/删除保持顶层，分享/标签收纳到「更多操作」，插件统一进「插件」子菜单。
4. 默认设置审查：付费类功能（文件云同步）默认不开启，避免新装机首屏出现付费入口。
5. 关键事件本地埋点：首次记录、首次唤出、首次搜索、首次复制历史、首次收藏，仅本机存储不上报。

P1+：在 P0 收敛完成后，再继续推进集成测试补齐、插件权限说明、`REPLACE`/`SAVE` 结果一致性、Linux/Wayland 兼容性、错误提示与日志可读性。

## 6. 套餐与升级提示原则（P4）

### 6.1 套餐分层（与现网 pricing.html 对齐）

- 免费版：本地历史、搜索、收藏、热键、基础插件；30 条云端记录、2 台设备。
- Basic：多设备云同步、200 条云端记录、3 台设备、5 GB 文件同步、全文搜索。
- Super：500 条记录、5 台设备、50 GB 文件同步、分享链接（按权益开放）。
- Ultimate：1000 条记录、不限设备、200 GB 文件同步。

Team 档位是中期方向，当前 pricing 未上线。客户端不假设 Team 存在；遇到团队席位/团队空间走 entitlement 字段判断。

### 6.2 触发原则

升级提示只在用户触达限制时出现，不在主路径常驻：

- 侧栏不再常驻"了解云端增强"按钮；用户可在「设置 → 云端同步」主动查看 SubscriptionWidget。
- 文件云同步：触达配额满或接近满（≥85%）时，由顶部状态条提示一次（不重复打扰）；未付费用户点"添加文件"才弹"需要升级"对话框。
- 分享链接：用户点击「分享...」时若 `entitlement.can_share_link` 为假，弹场景化对话框解释能力 + 引导查看套餐；不再在右键菜单里灰化按钮。
- 团队空间：仅 `team_seats` / `is_team_owner` 为真时显示「管理团队」入口。

### 6.3 不要做的事

- 不在主界面、托盘、通知里推送营销型升级提示。
- 不用红点/弹窗推动登录或安装插件。
- 不把基础搜索、收藏、热键、低容量本地历史做成付费门槛。

## 6. 中期方向

如果现有基础继续稳定，后续更适合扩展的方向是：
- 更强的内容组织能力，例如标签、分类和收藏视图。
- 更丰富的插件配置控件。
- 更完整的云端插件商店和版本管理。
- 更细粒度的同步策略和冲突处理。
- 更清晰的企业场景能力，但应建立在现有本地优先架构上。

## 7. 目前不建议写进宣传文案的内容

- 任何未经验证的市场规模数字。
- 任何“已经完成但实际上只是规划”的功能。
- 任何把外部服务当成必需项的叙述。
- 任何和当前实现不一致的插件或产品名称。

## 8. 结论

SharedClipboard 当前最稳妥的产品叙事是：
- 一个能独立运行的跨平台剪贴板历史工具。
- 具备本地优先的数据存储和可选同步。
- 通过插件系统持续扩展能力。

接下来的产品策略应该围绕“稳定、可扩展、可控同步”继续推进，而不是先写大而全的市场故事。
